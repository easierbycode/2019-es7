// Entry for the <character-editor> custom element bundle. Importing this
// module registers the element (the .svelte file carries the tag).
import Editor from "./CharacterEditorElement.svelte";

export default Editor;

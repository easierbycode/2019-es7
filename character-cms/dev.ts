// Fresh 2.x uses Vite. Use `deno task dev` (which runs `vite`) for the
// dev server. This file exists for backward compatibility with `Builder`-
// based docs; it just delegates to vite.
//
//   deno task dev
//
// See vite.config.ts for plugin wiring.
import "vite";
